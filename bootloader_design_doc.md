# TI 28388D Bootloader 设计文档

## 1. 概述

本文档描述了TI 28388D芯片的三级Bootloader系统设计。该系统采用分层架构，实现安全可靠的固件更新和应用程序引导功能。

### 1.1 设计目标
- 提供安全可靠的固件更新机制
- 支持C28x主核和CM核的独立更新
- 确保系统在更新失败时的可恢复性
- 最小化固化代码，提高系统灵活性

### 1.2 系统组成
系统由三个独立项目组成：
1. **bl_mini** - 最小化一级Bootloader（固化）
2. **bl_main** - 主Bootloader（可更新）
3. **cm_agent** - CM核代理程序（可更新）

## 2. 内存布局设计

### 2.1 Flash存储器分配（C28x主核）

| 起始地址 | 结束地址 | 大小 | 用途 | 可更新性 |
|---------|---------|------|------|---------|
| 0x080000 | 0x081FFF | 8KB | bl_mini程序 | 否（固化） |
| 0x082000 | 0x083FFF | 8KB | bl_main程序 | 是 |
| 0x084000 | 0x085FFF | 8KB | 保留/扩展 | 是 |
| 0x086000 | 0x086FFF | 4KB | cm_agent程序 | 是 |
| 0x087000 | 0x087FFF | 4KB | cpu2_agent程序 | 是 |
| 0x088000 | 0x0AFFFF | 160KB | CPU1应用程序 | 是 |
| 0x0B0000 | 0x0BFFFF | 64KB | 保留/扩展 | 是 |

### 2.1.1 Flash存储器分配（CPU2核）

| 起始地址 | 结束地址 | 大小 | 用途 | 可更新性 |
|---------|---------|------|------|---------|
| 0x000000 | 0x07FFFF | 512KB | CPU2应用程序 | 是 |

### 2.1.2 Flash存储器分配（CM核）

| 起始地址 | 结束地址 | 大小 | 用途 | 可更新性 |
|---------|---------|------|------|---------|
| 0x000000 | 0x0FFFFF | 1MB | CM应用程序 | 是 |

### 2.2 RAM使用规划

#### C28x主核RAM
- **RAMM0/RAMM1**: bl_mini运行时栈和变量
- **RAMLS0-3**: bl_main运行时代码和数据
- **RAMLS4-7**: 应用程序运行时数据
- **RAMGS0-15**: 应用程序运行时数据和缓冲区

#### CM核RAM
- **CM专用RAM**: cm_agent运行时空间
- **共享RAM**: C28x与CM核通信区域

### 2.3 通信内存区域

| 区域 | 地址范围 | 用途 |
|-----|---------|------|
| CPU1TOCPU2RAM | 0x03A000 | CPU1到CPU2消息传递 |
| CPU2TOCPU1RAM | 0x03B000 | CPU2到CPU1消息传递 |
| CPUTOCMRAM | 0x039000 | CPU到CM核消息传递 |
| CMTOCPURAM | 0x038000 | CM核到CPU消息传递 |

## 3. 系统架构

### 3.1 三级Bootloader架构

```
┌─────────────────────────────────────────────────┐
│           系统上电/复位                          │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  bl_mini (FLASH0: 0x080000-0x081FFF)           │
│  - 最小化功能                                   │
│  - 固化不可更新                                 │
│  - 负责加载并跳转到bl_main                      │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  bl_main (FLASH1-2: 0x082000-0x083FFF)        │
│  - 主Bootloader功能                            │
│  - 应用程序引导                                 │
│  - 应用程序更新                                 │
│  - CM核更新协调                                 │
└─────────────────┬───────────────────────────────┘
                  │
         ┌────────┴────────┐
         │                 │
         ▼                 ▼
┌──────────────┐  ┌──────────────────┐
│   应用程序    │  │  cm_agent        │
│   (APP)      │  │  (CM核代理)      │
└──────────────┘  └──────────────────┘
```

### 3.2 模块职责

#### bl_mini模块
- 系统初始化（最小化）
- bl_main完整性校验
- bl_main加载到RAM
- 跳转到bl_main执行

#### bl_main模块
- 通信接口管理（UART/CAN等）
- 应用程序完整性校验
- 应用程序引导
- 应用程序固件更新
- CM核更新协调
- 错误处理和恢复

#### cm_agent模块
- 接收来自bl_main的固件数据
- CM核Flash擦除和编程
- 更新状态反馈
- 自更新功能

## 4. 启动流程设计

### 4.1 正常启动流程

```
1. 系统上电/复位
   ↓
2. bl_mini启动
   - 基本硬件初始化
   - 检查bl_main有效性
   ↓
3. bl_main校验
   - 读取bl_main的CRC校验值
   - 验证完整性
   ↓
4. 加载bl_main
   - 将bl_main从Flash复制到RAM
   - 设置栈指针
   ↓
5. 跳转到bl_main
   - 跳转到bl_main入口点
   ↓
6. bl_main启动
   - 完整硬件初始化
   - 初始化通信接口
   - 检查更新标志
   ↓
7. 检查更新状态
   - 无更新标志 → 进入程序引导流程
   - 有更新标志 → 进入更新模式
   ↓
8. 程序CRC校验流程
   - 校验bl_main自身CRC
   - 校验CPU1应用程序CRC (FLASH4-9)
   - 校验CPU2应用程序CRC (FLASH10-13)
   - 校验CM应用程序CRC (0x0C0000-0x0FFFFF)
   ↓
9. CRC校验结果判断
   - 所有程序CRC校验通过 → 继续引导
   - 任一程序CRC校验失败 → 进入错误处理
   ↓
10. 应用程序引导
    - 加载CPU1应用程序到RAM
    - 启动CPU2应用程序
    - 启动CM应用程序
    - 跳转到CPU1应用程序入口点
```

### 4.2 更新模式启动流程

```
1. bl_main检测到更新标志
   ↓
2. 进入更新模式
   - 等待上位机连接
   - 接收更新数据包
   ↓
3. 固件更新
   - 擦除目标Flash区域
   - 编程新固件
   - 校验写入数据
   ↓
4. 更新完成
   - 清除更新标志
   - 重启系统
```

### 4.3 CM核更新流程

```
1. bl_main接收到CM核更新请求
   ↓
2. 加载cm_agent到CM核RAM
   - 从FLASH3读取cm_agent
   - 通过IPC传输到CM核RAM
   ↓
3. 启动CM核
   - 通过IPC唤醒CM核
   - CM核开始执行cm_agent
   ↓
4. 数据传输
   - bl_main通过通信接口接收固件数据
   - 通过IPC转发给cm_agent
   ↓
5. cm_agent执行更新
   - 擦除CM核Flash
   - 编程新固件
   - 校验写入数据
   ↓
6. 更新完成
   - cm_agent发送完成信号
   - bl_main重启CM核
```

### 4.4 CRC校验机制

#### 4.4.1 CRC校验范围

| 程序名称 | Flash区域 | 起始地址 | 结束地址 | 大小 |
|---------|----------|---------|---------|------|
| bl_main | FLASH1-2 | 0x082000 | 0x083FFF | 8KB |
| cm_agent | FLASH3 | 0x086000 | 0x086FFF | 4KB |
| cpu2_agent | FLASH3 | 0x087000 | 0x087FFF | 4KB |
| CPU1应用程序 | FLASH4-9 | 0x088000 | 0x0AFFFF | 160KB |
| CPU2应用程序 | CPU2 Flash | 0x000000 | 0x07FFFF | 512KB |
| CM应用程序 | CM Flash | 0x000000 | 0x0FFFFF | 1MB |

#### 4.4.2 CRC校验流程

```
1. bl_main启动后执行CRC校验
   ↓
2. 读取各程序的CRC校验值
   - 从固定位置读取存储的CRC值
   ↓
3. 计算实际CRC值
   - 遍历程序Flash区域
   - 计算CRC32校验值
   ↓
4. 比较CRC值
   - 比较存储值和计算值
   ↓
5. 校验结果处理
   - 全部通过 → 继续启动流程
   - 任一失败 → 进入错误处理
```

**校验顺序**：
1. bl_main自身CRC校验
2. cm_agent CRC校验
3. cpu2_agent CRC校验
4. CPU1应用程序CRC校验
5. CPU2应用程序CRC校验
6. CM应用程序CRC校验

#### 4.4.3 CRC值存储位置

| 程序名称 | CRC存储地址 | 说明 |
|---------|------------|------|
| bl_main | 0x083FFC | FLASH2末尾4字节 |
| cm_agent | 0x086FFC | FLASH3前4KB末尾4字节 |
| cpu2_agent | 0x087FFC | FLASH3后4KB末尾4字节 |
| CPU1应用程序 | 0x0AFFFC | FLASH9末尾4字节 |
| CPU2应用程序 | 0x07FFFC | CPU2 Flash末尾4字节 |
| CM应用程序 | 0x0FFFFC | CM Flash末尾4字节 |

#### 4.4.4 CRC校验失败处理

```
1. 检测到CRC校验失败
   ↓
2. 记录错误信息
   - 记录失败的程序名称
   - 记录期望CRC值
   - 记录实际CRC值
   ↓
3. 根据失败类型处理
   - bl_main失败 → 停止启动，进入安全模式
   - 应用程序失败 → 跳过该程序，尝试启动其他程序
   ↓
4. 通过通信接口上报错误
   - 发送错误状态码
   - 发送详细错误信息
   ↓
5. 等待用户干预或进入更新模式
```

#### 4.4.5 CRC计算规范

- **算法**: CRC32
- **多项式**: 0x04C11DB7
- **初始值**: 0xFFFFFFFF
- **结果异或**: 0xFFFFFFFF
- **输入反转**: 是
- **输出反转**: 是

## 5. 通信协议设计

### 5.1 通信接口选择
- **主通信接口**: UART（调试和更新）
- **备选接口**: CAN（工业环境）

### 5.2 数据包格式

#### 请求包格式
```
[起始符][命令][长度][数据][校验][结束符]
```

#### 响应包格式
```
[起始符][状态][长度][数据][校验][结束符]
```

### 5.3 命令定义

| 命令码 | 功能 | 说明 |
|-------|------|------|
| 0x01 | 获取版本信息 | 返回bootloader版本 |
| 0x02 | 跳转到应用程序 | 引导应用程序 |
| 0x03 | 擦除Flash | 擦除指定区域 |
| 0x04 | 写入数据 | 写入固件数据 |
| 0x05 | 校验数据 | 校验写入的数据 |
| 0x06 | 更新CM核 | 启动CM核更新流程 |
| 0x07 | 更新cm_agent | 更新CM核代理程序 |
| 0x08 | 更新CPU2核 | 启动CPU2核更新流程 |
| 0x09 | 更新cpu2_agent | 更新CPU2核代理程序 |
| 0x0A | 重启系统 | 系统复位 |
| 0x0B | 获取状态 | 返回当前状态 |
| 0x0C | 计算CRC | 计算指定区域的CRC值 |
| 0x0D | 写入CRC | 写入CRC校验值到指定位置 |

### 5.4 状态码定义

| 状态码 | 含义 |
|-------|------|
| 0x00 | 成功 |
| 0x01 | 命令不支持 |
| 0x02 | 校验失败 |
| 0x03 | Flash擦除失败 |
| 0x04 | Flash编程失败 |
| 0x05 | 数据长度错误 |
| 0x06 | 超时 |
| 0x07 | CM核通信失败 |
| 0x08 | CPU2核通信失败 |
| 0xFF | 未知错误 |

## 6. 更新机制设计

### 6.1 应用程序更新流程

```
1. 上位机发送擦除命令
   - 指定应用程序Flash区域
   ↓
2. bl_main擦除Flash
   - 调用Flash API擦除
   - 返回擦除结果
   ↓
3. 上位机发送固件数据
   - 分包发送
   - 每包包含地址和数据
   ↓
4. bl_main写入Flash
   - 验证数据包
   - 编程到指定地址
   - 返回写入结果
   ↓
5. 上位机发送校验命令
   - 指定校验范围
   ↓
6. bl_main执行校验
   - 计算校验和
   - 返回校验结果
   ↓
7. 更新完成
   - 设置更新完成标志
   - 等待重启命令
```

### 6.2 bl_main自更新流程

```
1. 上位机发送bl_main更新请求
   ↓
2. bl_main检查更新条件
   - 验证权限
   - 检查Flash空间
   ↓
3. 擦除FLASH1-2区域
   ↓
4. 接收新bl_main固件
   - 直接编程到Flash
   ↓
5. 校验新固件
   - 计算CRC值
   - 验证完整性
   ↓
6. 写入CRC值
   - 将CRC值写入0x083FFC
   ↓
7. 设置重启标志
   ↓
8. 重启系统
   - bl_mini加载新bl_main
```

### 6.3 CM核更新流程

```
1. 上位机发送CM核更新请求
   ↓
2. bl_main准备cm_agent
   - 从FLASH3读取cm_agent
   ↓
3. 传输cm_agent到CM核RAM
   - 通过IPC传输
   ↓
4. 启动CM核
   - 通过IPC唤醒
   ↓
5. CM核执行cm_agent
   - 等待固件数据
   ↓
6. bl_main转发固件数据
   - 接收上位机数据
   - 通过IPC转发
   ↓
7. cm_agent更新CM核Flash
   - 擦除
   - 编程
   - 校验
   ↓
8. 更新完成
   - cm_agent返回状态
   - bl_main重启CM核
```

### 6.4 cm_agent自更新流程

```
1. 上位机发送cm_agent更新请求
   ↓
2. bl_main擦除FLASH3前4KB区域 (0x086000-0x086FFF)
   ↓
3. 接收新cm_agent固件
   - 直接编程到FLASH3前4KB
   ↓
4. 校验新cm_agent
   - 计算CRC值
   - 写入CRC值到0x086FFC
   - 验证完整性
   ↓
5. 更新完成
   - cm_agent已更新到FLASH3
   - 下次CM核更新时使用新版本
```

### 6.4.1 cpu2_agent自更新流程

```
1. 上位机发送cpu2_agent更新请求
   ↓
2. bl_main擦除FLASH3后4KB区域 (0x087000-0x087FFF)
   ↓
3. 接收新cpu2_agent固件
   - 直接编程到FLASH3后4KB
   ↓
4. 校验新cpu2_agent
   - 计算CRC值
   - 写入CRC值到0x087FFC
   - 验证完整性
   ↓
5. 更新完成
   - cpu2_agent已更新到FLASH3
   - 下次CPU2核更新时使用新版本
```

### 6.5 CPU1应用程序更新流程

```
1. 上位机发送擦除命令
   - 指定CPU1应用程序Flash区域 (FLASH4-9)
   ↓
2. bl_main擦除Flash
   - 调用Flash API擦除
   - 返回擦除结果
   ↓
3. 上位机发送固件数据
   - 分包发送
   - 每包包含地址和数据
   ↓
4. bl_main写入Flash
   - 验证数据包
   - 编程到指定地址
   - 返回写入结果
   ↓
5. 上位机发送写入CRC命令
   - 指定CRC值和目标地址
   ↓
6. bl_main写入CRC值
   - 将CRC值写入0x0AFFFC
   ↓
7. 更新完成
   - 等待重启命令
```

### 6.6 CPU2应用程序更新流程

```
1. 上位机发送CPU2应用程序更新请求
   ↓
2. bl_main准备cpu2_agent
   - 从FLASH3读取cpu2_agent (0x087000-0x087FFF)
   ↓
3. 传输cpu2_agent到CPU2核RAM
   - 通过IPC传输
   ↓
4. 启动CPU2核
   - 通过IPC唤醒
   ↓
5. CPU2核执行cpu2_agent
   - 等待固件数据
   ↓
6. bl_main转发固件数据
   - 接收上位机数据
   - 通过IPC转发
   ↓
7. cpu2_agent更新CPU2应用程序Flash
   - 擦除CPU2应用程序区域 (0x000000-0x07FFFF)
   - 编程新固件
   - 校验写入数据
   - 写入CRC值到0x07FFFC
   ↓
8. 更新完成
   - cpu2_agent返回状态
   - bl_main重启CPU2核
```

### 6.7 CM应用程序更新流程

```
1. 上位机发送CM应用程序更新请求
   ↓
2. bl_main准备cm_agent
   - 从FLASH3读取cm_agent (0x086000-0x086FFF)
   ↓
3. 传输cm_agent到CM核RAM
   - 通过IPC传输
   ↓
4. 启动CM核
   - 通过IPC唤醒
   ↓
5. CM核执行cm_agent
   - 等待固件数据
   ↓
6. bl_main转发固件数据
   - 接收上位机数据
   - 通过IPC转发
   ↓
7. cm_agent更新CM应用程序Flash
   - 擦除CM应用程序区域 (0x000000-0x0FFFFF)
   - 编程新固件
   - 校验写入数据
   - 写入CRC值到0x0FFFFC
   ↓
8. 更新完成
   - cm_agent返回状态
   - bl_main重启CM核
```

## 7. 安全性设计

### 7.1 完整性校验
- **CRC校验**: 所有固件使用CRC32校验
- **启动校验**: 每次启动都校验固件完整性
- **更新校验**: 更新完成后校验新固件完整性

### 7.2 更新保护
- **原子更新**: 确保更新过程的原子性
- **CRC验证**: 更新前验证CRC值
- **写入验证**: 编程后验证写入数据

### 7.3 访问控制
- **更新权限**: 需要特定命令才能进入更新模式
- **超时保护**: 更新超时自动终止

### 7.4 错误恢复
- **看门狗**: 防止系统死锁
- **错误计数**: 记录更新失败次数
- **安全模式**: 多次失败后进入安全模式

## 8. IPC通信设计

### 8.1 IPC通信概述

bl_main与CM核之间的通信通过IPC（Inter-Processor Communication）实现，主要用于：
- 传输cm_agent到CM核RAM
- 转发固件更新数据
- 传递更新状态和命令

### 8.2 IPC消息格式

#### 8.2.1 消息头结构

```c
typedef struct {
    uint32_t msgType;       // 消息类型
    uint32_t msgLen;        // 数据长度
    uint32_t seqNum;        // 序列号
    uint32_t reserved;      // 保留字段
} IPC_MsgHeader_t;
```

#### 8.2.2 消息类型定义

| 消息类型 | 值 | 说明 |
|---------|---|------|
| IPC_MSG_CMD | 0x01 | 命令消息 |
| IPC_MSG_DATA | 0x02 | 数据消息 |
| IPC_MSG_ACK | 0x03 | 应答消息 |
| IPC_MSG_STATUS | 0x04 | 状态消息 |

#### 8.2.3 命令类型定义

| 命令类型 | 值 | 说明 |
|---------|---|------|
| IPC_CMD_START_UPDATE | 0x01 | 开始更新 |
| IPC_CMD_WRITE_DATA | 0x02 | 写入数据 |
| IPC_CMD_VERIFY_CRC | 0x03 | 验证CRC |
| IPC_CMD_FINISH_UPDATE | 0x04 | 完成更新 |
| IPC_CMD_RESTART | 0x05 | 重启CM核 |

### 8.3 IPC通信流程

#### 8.3.1 cm_agent加载流程

```
1. bl_main读取cm_agent
   - 从FLASH3读取cm_agent二进制数据
   ↓
2. bl_main准备IPC消息
   - 创建IPC_MSG_CMD消息
   - 命令类型: IPC_CMD_START_UPDATE
   ↓
3. bl_main发送启动命令
   - 通过CPUTOCMRAM发送消息
   - 等待CM核应答
   ↓
4. bl_main传输cm_agent代码
   - 分块传输cm_agent数据
   - 每块使用IPC_MSG_DATA消息
   - 等待每块的ACK
   ↓
5. CM核确认接收完成
   - 通过CMTOCPURAM发送完成消息
   ↓
6. bl_main启动CM核
   - 通过IPC唤醒CM核
   - CM核开始执行cm_agent
```

#### 8.3.2 固件数据传输流程

```
1. bl_main接收上位机固件数据
   ↓
2. bl_main准备IPC数据消息
   - 创建IPC_MSG_DATA消息
   - 包含固件数据和地址信息
   ↓
3. bl_main发送数据到CM核
   - 通过CPUTOCMRAM发送消息
   ↓
4. CM核接收并处理数据
   - cm_agent读取消息
   - 编程到CM核Flash
   ↓
5. CM核发送应答
   - 通过CMTOCPURAM发送IPC_MSG_ACK
   ↓
6. bl_main继续发送下一包数据
   - 循环直到所有数据发送完成
```

#### 8.3.3 更新完成流程

```
1. bl_main发送完成命令
   - 创建IPC_MSG_CMD消息
   - 命令类型: IPC_CMD_FINISH_UPDATE
   ↓
2. CM核执行最终校验
   - cm_agent验证整个固件
   - 计算CRC值
   ↓
3. CM核返回状态
   - 通过CMTOCPURAM发送IPC_MSG_STATUS
   - 包含更新结果和CRC值
   ↓
4. bl_main重启CM核
   - 发送IPC_CMD_RESTART命令
   - CM核重启运行新固件
```

### 8.4 IPC通信错误处理

#### 8.4.1 超时处理

```
1. 发送消息后启动超时定时器
   ↓
2. 等待应答消息
   ↓
3. 超时检测
   - 超时未收到应答 → 重发消息
   - 重发次数超限 → 终止通信，上报错误
```

#### 8.4.2 序列号检查

```
1. 接收消息时检查序列号
   ↓
2. 验证序列号连续性
   ↓
3. 序列号异常处理
   - 请求重发
   - 或重新建立通信
```

#### 8.4.3 数据完整性检查

```
1. 每个消息包含校验和
   ↓
2. 接收时验证校验和
   ↓
3. 校验失败处理
   - 请求重发
   - 重发次数超限 → 终止通信
```

### 8.5 IPC内存使用

#### 8.5.1 CPUTOCMRAM使用

| 偏移 | 大小 | 用途 |
|-----|------|------|
| 0x00 | 16 | 消息头 |
| 0x10 | 1008 | 消息数据 |
| 0x400 | 16 | 控制标志 |
| 0x410 | 16 | 状态标志 |

#### 8.5.2 CMTOCPURAM使用

| 偏移 | 大小 | 用途 |
|-----|------|------|
| 0x00 | 16 | 消息头 |
| 0x10 | 1008 | 消息数据 |
| 0x400 | 16 | 控制标志 |
| 0x410 | 16 | 状态标志 |

## 9. 错误处理机制

### 9.1 错误分类

#### 致命错误
- Flash擦除/编程失败
- 校验失败
- 通信超时

#### 可恢复错误
- 数据包校验错误（重传）
- 临时通信中断（重连）

#### 警告
- 版本不匹配
- 配置参数异常

### 9.2 错误处理策略

#### Flash操作错误
```
1. 检测到错误
   ↓
2. 记录错误日志
   ↓
3. 尝试重新操作
   ↓
4. 仍然失败 → 终止更新，上报错误
```

#### 通信错误
```
1. 检测到错误
   ↓
2. 请求重传
   ↓
3. 重传次数超限 → 终止更新
```

#### 校验错误
```
1. 检测到校验失败
   ↓
2. 重新校验
   ↓
3. 仍然失败 → 拒绝更新
```

### 9.3 错误日志
- 记录错误类型
- 记录错误时间
- 记录相关参数
- 保存到非易失性存储

## 10. 项目结构

### 10.1 bl_mini项目
```
bl_mini/
├── bl_mini.c              # 主程序
├── bl_mini.h              # 头文件
├── 2838x_flash_lnk_cpu1.cmd  # 链接脚本
├── syscfg/                # 系统配置
└── device/                # 设备驱动
```

### 10.2 bl_main项目
```
bl_main/
├── bl_main.c              # 主程序
├── bl_main.h              # 头文件
├── comm.c                 # 通信模块
├── flash_ops.c            # Flash操作
├── update.c               # 更新逻辑
├── cm_update.c            # CM核更新
├── 2838x_flash_lnk_cpu1.cmd  # 链接脚本
└── syscfg/                # 系统配置
```

### 10.3 cm_agent项目
```
cm_agent/
├── cm_agent.c             # 主程序
├── cm_agent.h             # 头文件
├── flash_ops.c            # Flash操作
├── ipc_handler.c          # IPC处理
└── cm_link.cmd            # CM核链接脚本
```

## 11. 开发和测试计划

### 11.1 开发阶段
1. **Phase 1**: bl_mini开发
2. **Phase 2**: bl_main基础功能
3. **Phase 3**: 应用程序更新功能
4. **Phase 4**: CM核更新功能
5. **Phase 5**: 安全性和错误处理

### 11.2 测试计划
1. **单元测试**: 各模块独立测试
2. **集成测试**: 模块间协作测试
3. **系统测试**: 完整流程测试
4. **压力测试**: 长时间运行测试
5. **异常测试**: 错误恢复测试

## 12. 待完善事项

### 12.1 需要进一步讨论的问题
1. 通信协议的具体细节（后续补充）
2. 固件版本管理机制
3. 错误日志的存储位置
4. 更新超时的时间设置
5. CM核Flash的具体地址范围

### 12.2 需要确认的技术细节
1. Flash擦除和编程的具体API
2. IPC通信的具体实现和消息格式
3. CM核启动和停止的机制
4. 看门狗的配置
5. 中断处理策略

## 13. 附录

### 13.1 TI 28388D Flash扇区信息
- FLASH0: 0x080000, 8KB
- FLASH1: 0x082000, 8KB
- FLASH2: 0x084000, 8KB
- FLASH3: 0x086000, 8KB
- FLASH4-9: 0x088000-0x0B7FFF, 各32KB
- FLASH10-13: 0x0B8000-0x0BFFFF, 各8KB

### 13.3 CRC计算实现参考

#### 13.3.1 CRC32计算函数示例

```c
uint32_t calculateCRC32(uint32_t startAddr, uint32_t length)
{
    uint32_t crc = 0xFFFFFFFF;
    uint32_t i, j;
    uint8_t data;

    for (i = 0; i < length; i++) {
        data = *((uint8_t *)(startAddr + i));
        crc ^= data;
        for (j = 0; j < 8; j++) {
            if (crc & 1) {
                crc = (crc >> 1) ^ 0xEDB88320;
            } else {
                crc >>= 1;
            }
        }
    }

    return ~crc;
}
```

#### 13.3.2 CRC校验函数示例

```c
bool verifyCRC(uint32_t startAddr, uint32_t length, uint32_t expectedCRC)
{
    uint32_t calculatedCRC = calculateCRC32(startAddr, length);
    return (calculatedCRC == expectedCRC);
}
```

### 13.2 相关文档
- TI 28388D Technical Reference Manual
- TI C2000 Flash API User's Guide
- TI IPC Driver User's Guide

---

**文档版本**: 1.1
**创建日期**: 2025-12-29
**最后更新**: 2025-12-29
